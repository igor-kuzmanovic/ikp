#include "Protocol.h"

int SerializeMessage(Message* message, char* buffer) {
    // TODO Insanity

    return -1;
}


int DeserializeMessage(char* buffer, Message* message) {
    // TODO Insanity

    return -1;
}
